#env
.env
.env.*
!.env.example

# Logs
/logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env
.vite

# Claude Code session data
.claude/

# local env
.env.local

# Capacitor native build output
android/app/build/
android/.gradle/
ios/App/Pods/
ios/App/build/

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Obsidian vault artifacts from opening this folder in Obsidian. Not part of
# the codebase and not created by any build step. Ignored so a `git add -A`
# cannot sweep them into a commit — which is exactly what nearly happened
# during the 2026-08-25 security pass.
.obsidian/
/gamefit-app/
